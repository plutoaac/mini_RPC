# RPC Server 流程图

本文档详细描述了 RPC 服务器的架构和运行流程。

## 1. 整体架构图

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                                   RPC Server                                     │
│                                                                                  │
│  ┌────────────────────────────────────────────────────────────────────────────┐ │
│  │                              RpcServer                                      │ │
│  │                                                                             │ │
│  │   ┌─────────────┐    ┌─────────────┐    ┌───────────────────────────────┐  │ │
│  │   │ listen_fd   │    │ epoll_fd    │    │     connections (map)         │  │ │
│  │   │ (监听socket)│    │ (epoll实例) │    │  fd -> Connection             │  │ │
│  │   └─────────────┘    └─────────────┘    └───────────────────────────────┘  │ │
│  └────────────────────────────────────────────────────────────────────────────┘ │
│                                                                                  │
│                              epoll 事件循环                                      │
│                                                                                  │
│              EPOLLIN  ──────> 新连接到达 / 数据可读                              │
│              EPOLLOUT ──────> 可以发送数据                                       │
│              EPOLLRDHUP ────> 对端关闭连接                                       │
│                                                                                  │
└──────────────────────────────────────────────────────────────────────────────────┘

                               数据流向
    ─────────────────────────────────────────────────────────────────────
    
    客户端请求:  [Client] ──recv()──> read_buffer_ ──解析帧──> RpcRequest
                                                                   │
                                                                   ▼
                                                            ServiceRegistry
                                                                   │
                                                                   ▼
    服务端响应:  [Client] <──send()── write_buffer_ <──编码帧── RpcResponse
```

## 2. 服务器启动流程

```
                              ┌─────────────────────┐
                              │   Start() 开始      │
                              └──────────┬──────────┘
                                         │
                                         ▼
                    ┌────────────────────────────────────────┐
                    │           初始化阶段                    │
                    │                                        │
                    │  1. 创建 TCP socket (listen_fd)        │
                    │  2. 设置 SO_REUSEADDR                  │
                    │  3. 设置非阻塞模式 (O_NONBLOCK)         │
                    │  4. 绑定端口 (bind)                    │
                    │  5. 开始监听 (listen, backlog=128)     │
                    │  6. 创建 epoll 实例 (epoll_fd)         │
                    │  7. 注册 listen_fd 到 epoll            │
                    │                                        │
                    └──────────────────┬─────────────────────┘
                                       │
                              ┌────────┴────────┐
                              │   初始化失败?   │
                              └────────┬────────┘
                                是 │       │ 否
                                   ▼       ▼
                         ┌─────────────┐  ┌──────────────────────┐
                         │ return false│  │ 进入 epoll 事件循环  │
                         └─────────────┘  └──────────────────────┘
```

## 3. epoll 事件循环

```
┌──────────────────────────────────────────────────────────────────────────────────┐
│                              主事件循环 (无限循环)                                 │
│                                                                                   │
│                epoll_wait(events, 64, timeout=-1)                                │
│                      等待事件发生 (阻塞)                                          │
│                                                                                   │
│                             ┌────────┴────────┐                                   │
│                             │  事件就绪?      │                                   │
│                             └────────┬────────┘                                   │
│                           是 │             │ 否 (错误)                             │
│                              ▼             ▼                                       │
│                    ┌──────────────┐  ┌──────────────────┐                         │
│                    │ 遍历 events  │  │ EINTR: continue  │                         │
│                    └──────┬───────┘  │ 其他: return     │                         │
│                           │          └──────────────────┘                         │
│                           ▼                                                        │
│   ┌───────────────────────────────────────────────────────────────────────────┐  │
│   │                         事件类型判断                                       │  │
│   │                                                                           │  │
│   │        fd == listen_fd ?                                                  │  │
│   │      ┌─────┴─────┐                                                        │  │
│   │      ▼           ▼                                                        │  │
│   │  ┌───────┐   ┌────────────────────────────────────────┐                    │  │
│   │  │新连接 │   │          客户端 socket 事件             │                    │  │
│   │  │处理   │   │                                        │                    │  │
│   │  └───┬───┘   │  EPOLLIN  ──> OnReadable()            │                    │  │
│   │      │       │  EPOLLOUT ──> OnWritable()            │                    │  │
│   │      │       │  EPOLLERR/HUP/RDHUP ──> MarkClosing() │                    │  │
│   │      │       │  更新 epoll 事件 (EPOLL_CTL_MOD)       │                    │  │
│   │      │       └────────────────────────────────────────┘                    │  │
│   │      ▼                                                                      │  │
│   │  ┌──────────────────────────────────────────────────────────────────┐      │  │
│   │  │                    Accept 循环 (非阻塞)                           │      │  │
│   │  │                                                                  │      │  │
│   │  │  while (true) {                                                  │      │  │
│   │  │      client_fd = accept4(SOCK_NONBLOCK | SOCK_CLOEXEC)           │      │  │
│   │  │      if (EAGAIN) break;   // 没有更多连接                        │      │  │
│   │  │      epoll_ctl(EPOLL_CTL_ADD, client_fd, EPOLLIN|EPOLLRDHUP)    │      │  │
│   │  │      connections.emplace(client_fd, Connection(fd, registry))   │      │  │
│   │  │  }                                                               │      │  │
│   │  └──────────────────────────────────────────────────────────────────┘      │  │
│   └───────────────────────────────────────────────────────────────────────────┘  │
│                                                                                   │
│                              (循环继续，等待下一个事件)                            │
└───────────────────────────────────────────────────────────────────────────────────┘
```

## 4. Connection 数据读取流程 (OnReadable)

```
                              ┌─────────────────────┐
                              │   OnReadable()      │
                              └──────────┬──────────┘
                                         │
                                         ▼
                    ┌────────────────────────────────────────┐
                    │        ReadFromSocket()                │
                    │        从 socket 读取数据              │
                    └──────────────────┬─────────────────────┘
                                       │
                              ┌────────┴────────┐
                              │    读取结果?    │
                              └────────┬────────┘
              ┌─────────────────┬──────┴──────┬─────────────────┐
              │                 │             │                 │
              ▼                 ▼             ▼                 ▼
        ┌──────────┐     ┌───────────┐ ┌───────────┐     ┌──────────┐
        │ kData    │     │kPeerClosed│ │ kError    │     │kWouldBlock│
        │ 有数据   │     │对端关闭   │ │读取错误   │     │无数据可读│
        └────┬─────┘     └─────┬─────┘ └─────┬─────┘     └────┬─────┘
             │                 │             │                │
             │                 ▼             ▼                │
             │           ┌───────────┐ ┌───────────┐          │
             │           │关闭连接   │ │return     │          │
             │           │return true│ │false      │          │
             │           └───────────┘ └───────────┘          │
             │                                            │
             └────────────────────────────────────────────┤
                                                          │
                                                          ▼
                                        ┌──────────────────────────────┐
                                        │    TryParseRequests()        │
                                        │    解析并处理所有完整请求帧  │
                                        └──────────────┬───────────────┘
                                                       │
                                              ┌────────┴────────┐
                                              │     成功?       │
                                              └────────┬────────┘
                                                否 │       │ 是
                                                   ▼       └──────┐
                                         ┌─────────────┐          │
                                         │return false │          │
                                         └─────────────┘          │
                                                                  ▼
                                                        ┌──────────────┐
                                                        │ return true  │
                                                        └──────────────┘
```

## 5. 请求解析流程 (TryParseRequests)

```
                              ┌─────────────────────────┐
                              │ TryParseRequests() 开始 │
                              └────────────┬────────────┘
                                           │
                                           ▼
                        ┌─────────────────────────────────────┐
                        │  检查: 有足够字节读取帧头? (>= 4)   │
                        └───────────────────┬─────────────────┘
                                            │
                                 ┌──────────┴──────────┐
                                 │      有帧头?        │
                                 └──────────┬──────────┘
                                   否 │           │ 是
                                      ▼           ▼
                            ┌──────────────┐  ┌─────────────────────────┐
                            │ 移除已处理数据│  │ DecodeFrameHeader()     │
                            │ return true  │  │ 解析帧头获取 body_length│
                            └──────────────┘  └───────────┬─────────────┘
                                                        │
                                              ┌────────┴────────┐
                                              │  有完整消息体?  │
                                              └────────┬────────┘
                                                否 │       │ 是
                                                   ▼       ▼
                                      ┌──────────────┐  ┌───────────────────────┐
                                      │ 等待更多数据 │  │ 提取消息体            │
                                      │ break        │  │ 解析 RpcRequest       │
                                      └──────────────┘  └───────────┬───────────┘
                                                                  │
                                                        ┌────────┴────────┐
                                                        │   解析成功?     │
                                                        └────────┬────────┘
                                                          否 │       │ 是
                                                             ▼       ▼
                                               ┌─────────────────┐  ┌─────────────────┐
                                               │ 错误响应        │  │HandleOneRequest │
                                               │ PARSE_ERROR     │  │ 查找并调用方法  │
                                               └────────┬────────┘  └────────┬────────┘
                                                        │                    │
                                                        └─────────┬──────────┘
                                                                  ▼
                                                    ┌───────────────────────────┐
                                                    │ QueueResponse(response)   │
                                                    │ 将响应加入发送队列        │
                                                    └───────────────────────────┘
```

## 6. 单个请求处理流程 (HandleOneRequest)

```
                              ┌───────────────────────────────┐
                              │ HandleOneRequest() 开始       │
                              └───────────────┬───────────────┘
                                              │
                                              ▼
                              ┌───────────────────────────────┐
                              │ registry_.Find(service,method)│
                              └───────────────┬───────────────┘
                                              │
                                   ┌──────────┴──────────┐
                                   │     找到方法?       │
                                   └──────────┬──────────┘
                                     否 │           │ 是
                                        ▼           ▼
                          ┌─────────────────────┐  ┌───────────────────────────┐
                          │ 设置错误响应        │  │ try {                     │
                          │ METHOD_NOT_FOUND    │  │   handler(payload)        │
                          │ return true         │  │   设置 response.payload   │
                          └─────────────────────┘  │   return true             │
                                                   └─────────────┬─────────────┘
                                                                 │
                                                      ┌──────────┴──────────┐
                                                      │    抛出异常?        │
                                                      └──────────┬──────────┘
                                                    是 │           │ 否
                                                       ▼           ▼
                                         ┌─────────────────────┐  ┌──────────────┐
                                         │ 捕获异常            │  │ return true  │
                                         │ 设置错误响应        │  └──────────────┘
                                         │ return true         │
                                         └─────────────────────┘
```

## 7. 协议帧格式

```
┌──────────────────────────────────────────────────────────────────────────┐
│                           RPC 协议帧格式                                  │
├──────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│    0          4                                                         │
│    │<─────────>│<────────────────────────────────────────────────>│      │
│    │           │                                                │      │
│    │  帧头     │              帧体 (Body)                       │      │
│    │  4 字节   │              N 字节                            │      │
│    │           │                                                │      │
│    │  大端序   │              Protobuf 序列化数据               │      │
│    │  长度值   │              (RpcRequest 或 RpcResponse)       │      │
│    └───────────┴────────────────────────────────────────────────┘      │
│                                                                          │
│    限制: 0 < N <= 4MB (kMaxFrameSize)                                   │
│                                                                          │
└──────────────────────────────────────────────────────────────────────────┘
```

## 8. 完整请求处理时序图

```
┌──────────┐     ┌──────────┐     ┌────────────┐     ┌────────────────┐
│  Client  │     │RpcServer │     │ Connection │     │ServiceRegistry │
└────┬─────┘     └────┬─────┘     └─────┬──────┘     └───────┬────────┘
     │                │                 │                    │
     │  TCP Connect   │                 │                    │
     │───────────────>│                 │                    │
     │                │ accept4()       │                    │
     │                │ 创建 Connection │                    │
     │                │                 │                    │
     │  RPC Request   │                 │                    │
     │ (帧:长度+数据) │                 │                    │
     │────────────────────────────────>│                    │
     │                │                 │ OnReadable()       │
     │                │                 │ ReadFromSocket()   │
     │                │                 │ TryParseRequests() │
     │                │                 │ Find(service,method)
     │                │                 │───────────────────>│
     │                │                 │<───────────────────│
     │                │                 │   handler          │
     │                │                 │ HandleOneRequest() │
     │                │                 │ QueueResponse()    │
     │  RPC Response  │                 │                    │
     │ (帧:长度+数据) │                 │ OnWritable()       │
     │<────────────────────────────────│                    │
     │                │                 │                    │
     │  Close         │                 │                    │
     │────────────────────────────────>│                    │
     │                │                 │ EPOLLRDHUP         │
     │                │ close_connection│                    │
     │                │                 │                    │
     ▼                ▼                 ▼                    ▼
```

## 9. 核心组件职责

| 组件                | 职责                                   | 关键方法                        |
| ------------------- | -------------------------------------- | ------------------------------- |
| **RpcServer**       | 服务器生命周期管理，acceptor 事件循环  | `Start()`                       |
| **WorkerLoop**      | Worker 线程 epoll 循环，协程/线程池协调| `PollOnce()`, `DispatchRequestToThreadPool()` |
| **Connection**      | 单连接协议解析，双游标读缓冲，分片写缓冲| `OnReadable()`, `OnWritable()`  |
| **ServiceRegistry** | 服务方法注册与查找，线程安全           | `Register()`, `Find()`          |
| **ThreadPool**      | 业务线程池，per-worker MpscRingQueue   | `Submit()`, `Stop()`, `Join()`  |

## 10. 设计特点

1. **Acceptor + WorkerLoop 分层**：Acceptor 只 accept + 分发，Worker one-loop-per-thread 驱动连接
2. **C++20 协程驱动**：每连接一个 `HandleConnectionCo` 协程，epoll 事件驱动挂起/恢复
3. **业务线程池 + owner-worker 回投**：慢 handler 从 I/O worker 解耦，通过 `CompletedQueue` + eventfd 唤醒
4. **双游标 Buffer**：读路径 `read_index`/`write_index` 避免 O(N) memmove
5. **分片写缓冲**：`deque<PendingChunk>` + `pending_write_bytes_` O(1) 背压
6. **无锁 MPSC 任务队列**：ThreadPool 每 worker 独占 MpscRingQueue，CAS 入队，批量出队
7. **RAII 资源管理**：`UniqueFd` 自动管理 socket 生命周期
8. **动态事件注册**：根据写缓冲区状态动态调整 EPOLLOUT 事件
